<% content_for :title, "#{@abc_worksheet.title} - ABC Worksheet" %>
<%= turbo_frame_tag "main_content" do %>
  <%= render "abc_worksheets/show_content",
      abc_worksheet: @abc_worksheet,
      stuck_point: @stuck_point %>
<% end %>
