<div class="container mt-4 bg-white shadow p-4">
  <p style="color: green"><%= notice %></p>

  <h1>Stuck points</h1>

  <div id="stuck_points">
    <% @stuck_points.each do |stuck_point| %>
      <%= render stuck_point %>
      <p>
        <%= link_to "Edit", edit_index_event_stuck_point_path(@index_event, stuck_point) %> |
        <%= button_to "Delete", index_event_stuck_point_path(@index_event, stuck_point), method: :delete, data: { confirm: "Are you sure?" } %>
      </p>
    <% end %>
  </div>

  <%= link_to "New stuck point", new_index_event_stuck_point_path(@index_event) %>
</div>
